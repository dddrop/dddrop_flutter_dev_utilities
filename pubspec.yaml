name: dddrop_flutter_dev_utilities
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  flutter_hooks: ^0.18.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  hooks_riverpod: ^1.0.3
  filterable_logger: ^0.0.6

flutter:
